# LLM providers — set at least one
OPENAI_API_KEY=
GEMINI_API_KEY=

# Web scraping (optional — Jina Reader is used as free fallback when unset)
# Get a free key at https://www.firecrawl.dev  (500 pages/month free tier)
FIRECRAWL_API_KEY=
